services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - ./:/app
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      ENV: "debug"
    container_name: xyaod
    command: ["air", "-c", ".air.toml"]
