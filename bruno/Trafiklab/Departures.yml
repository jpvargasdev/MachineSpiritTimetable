info:
  name: Departures
  type: http
  seq: 3

http:
  method: GET
  url: https://transport.integration.sl.se/v1/sites/{{side_id}}/departures?transport=METRO
  params:
    - name: transport
      value: METRO
      type: query
  auth: inherit

runtime:
  variables:
    - name: side_id
      value: "9293"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
